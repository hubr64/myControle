import { StripMycontroleLinePipe } from './strip-mycontrole-line.pipe';

describe('StripMycontroleLinePipe', () => {
  it('create an instance', () => {
    const pipe = new StripMycontroleLinePipe();
    expect(pipe).toBeTruthy();
  });
});
